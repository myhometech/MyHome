import * as React from "react";

/** Minimal Toaster; replace later with your real toast provider */
export function Toaster(props: { children?: React.ReactNode }) {
  return <div data-stub="toaster" {...props} />;
}

export default Toaster;
